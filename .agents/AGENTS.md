# Workspace Customizations

These are rules specific to this workspace (`c:\studymate`).

## Coding Standards
- **File Length Limitation**: Every Dart file (`.dart`) in this project MUST NOT exceed 200 lines of code. Whenever writing or refactoring code, always split large files into smaller components placed in well-organized directories (e.g., `lib/screens/<feature>/widgets/` or `lib/widgets/`).
